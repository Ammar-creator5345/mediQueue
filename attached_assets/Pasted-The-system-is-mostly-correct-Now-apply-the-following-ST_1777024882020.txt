The system is mostly correct. Now apply the following STRICT UI + ROLE-BASED fixes.

Do NOT rebuild anything.
Only MODIFY existing code as instructed.

--------------------------------------------------

🚨 1. REMOVE FEE EDIT OPTION FROM ADMIN

Current issue:
- Admin can edit doctor fee

✅ FIX:

- Remove fee edit functionality from Admin panel completely
- Remove:
  - API call from admin side (if exists)
  - UI input field/button for fee in admin dashboard

- Admin should ONLY:
  - View doctor info
  - NOT edit consultation fee

--------------------------------------------------

🚨 2. DOCTOR CAN ONLY SEE HIS OWN QUEUE

Current issue:
- Doctor can see all doctors’ queues
- There is a selector to switch doctors

✅ FIX:

BACKEND:
- In queue API:
  - Use logged-in doctor ID from JWT
  - Return ONLY:
    queue where doctorId === logged-in doctor

FRONTEND:
- Remove doctor selector dropdown in "Live Queue"
- Doctor must NOT switch between doctors
- Show ONLY their own queue automatically

--------------------------------------------------

🚨 3. REMOVE FEE EDIT FROM DOCTOR OVERVIEW PAGE

Current issue:
- "My Consultation Fee" div exists in doctor overview

✅ FIX:

- Remove that entire div/component from overview page
- Overview should ONLY display info (read-only)

--------------------------------------------------

🚨 4. CREATE SEPARATE "DOCTOR PROFILE" PAGE

Add new page for doctor:

Route:
- /doctor/profile

Features:

Doctor can update:
- Name
- Speciality
- Consultation Fee
- Available timings (start time, end time)

--------------------------------------------------

BACKEND:

- Create/update API:
  PUT /api/doctors/profile

- Validate using Zod

--------------------------------------------------

FRONTEND:

- Create form using React Hook Form + Yup
- Pre-fill existing doctor data
- On submit:
  - Update data in DB
  - Show success message

--------------------------------------------------

🚨 5. SIDEBAR UPDATE (DOCTOR ROLE)

Add new menu item:
- "Profile" (Doctor Profile Page)

Remove any fee-edit related shortcuts from other pages

--------------------------------------------------

🚀 FINAL REQUIREMENTS

- Do NOT break existing working features
- Maintain role-based access
- No static data
- Ensure all updates reflect from MongoDB

--------------------------------------------------

⚠️ FINAL INSTRUCTION

Apply all these changes cleanly.
Do NOT ask questions.
Do NOT add extra features.
Only implement what is requested.