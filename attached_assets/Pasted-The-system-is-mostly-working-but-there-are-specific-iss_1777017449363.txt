The system is mostly working, but there are specific issues and feature gaps.

Do NOT rebuild the project.
Fix and extend the EXISTING system.

--------------------------------------------------

🚨 1. NOTIFICATIONS NOT WORKING (DOCTOR, ADMIN, RECEPTIONIST)

Current issue:
- Only patient notifications are working

✅ FIX:

Backend:
- Ensure Notification model supports all roles:
  - userId
  - message
  - type
  - role (patient, doctor, admin, receptionist)
  - isRead
  - createdAt

- Trigger notifications for ALL roles:

Doctor:
- When new patient added to queue
- When appointment assigned

Receptionist:
- When new appointment booked
- When queue updates

Admin:
- When new user/doctor added
- When reports generated

Frontend:
- Fetch notifications based on logged-in user ID
- Display in notification panel
- Add unread badge count
- Allow mark as read

--------------------------------------------------

🚨 2. DOCTOR QUEUE ISOLATION (VERY IMPORTANT)

Current issue:
- Doctors can see ALL queues

✅ FIX:

Backend:
- In queue API:
  - Filter by doctorId from JWT

Example:
- Doctor can ONLY fetch:
  queue where doctorId === logged-in doctor ID

Frontend:
- Doctor dashboard must ONLY show:
  - Their own patients queue

--------------------------------------------------

🚨 3. ADMIN REPORT: APPOINTMENT RECEIPT DOWNLOAD

Add new feature:

Admin can:
- View all appointments
- Download receipt (PDF)

Receipt must include:
- Patient Name
- Doctor Name
- Appointment Date
- Fee
- Token Number
- Status

✅ IMPLEMENT:

Backend:
- Create endpoint:
  GET /api/reports/receipt/:appointmentId

- Use PDF library (pdfkit or similar)

Frontend:
- Add "Download Receipt" button in admin panel
- Trigger download

--------------------------------------------------

🚨 4. DOCTOR FEE MANAGEMENT

Add dynamic doctor fee system

✅ BACKEND:

- Update Doctor model:
  - consultationFee (number)

- Add API:
  PUT /api/doctors/:id/fee

- Only Admin OR Doctor can update fee

--------------------------------------------------

✅ FRONTEND:

Doctor Profile Page:
- Show current fee
- Input field to update fee
- Submit → API call → update DB

Admin Panel:
- Option to update doctor fee

--------------------------------------------------

🚨 5. ENSURE DATA CONSISTENCY

- Appointment must store:
  - doctorId
  - patientId
  - fee (copied from doctor at booking time)

- Queue must reference:
  - doctorId

--------------------------------------------------

🚀 FINAL REQUIREMENTS

- Do NOT break existing features
- Do NOT use static data
- All updates must reflect in UI immediately
- Maintain role-based access

--------------------------------------------------

⚠️ FINAL INSTRUCTION

Fix all issues and implement all features above.
Do NOT ask questions.
Continue until everything is fully working.